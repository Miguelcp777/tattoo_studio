---
type: task-spec
id: TASK-0011
status: verified
created: 2026-09-19
---

# Task: Record first CI execution and the existence of a remote

Numbered outside the 0001–0010 range because those are reserved by the v0 plan for feature work.
This is housekeeping: no code changes, only evidence status corrections.

## Request, scope and current behavior

The repository gained a remote (`https://github.com/Miguelcp777/tattoo_studio`, public) and CI ran
for the first time. Until now the specs recorded, correctly, that CI had never executed and that
every check had only ever run on Windows. Those statements are now false and must be corrected.

Scope is evidence status only. No source file changes.

## Desired outcome and acceptance

- TASK-0011/REQ-001: Specs record the actual CI result rather than the previous `UNKNOWN`.
- TASK-0011/REQ-002: The `--base origin/main` guidance, previously marked unusable for lack of a
  remote, is corrected.
- TASK-0011/AC-001: `python scripts/check-spec-sync.py --base origin/main` exits 0.
- TASK-0011/AC-002: No material file changes in this task; the impact review contains spec rows
  only.

## Affected anchors and impact rationale

- `project-setup.spec.md` — CI execution and remote existence were both recorded as open.
- `platform.spec.md` — carried "CI has never executed" as its first listed debt item.
- `.specanchor/README.md` — stated that `--base origin/main` does not work.
- `project-codemap.md` — CI entry read "never executed".

## Verification evidence

| Requirement | Acceptance | Verification | Actual result | Evidence |
|---|---|---|---|---|
| TASK-0011/REQ-001 | TASK-0011/AC-001 | GitHub Actions run #1 at commit 652f938 | pass — 5/5 jobs success | EV-001 |
| TASK-0011/REQ-002 | TASK-0011/AC-001 | `check-spec-sync.py --base origin/main` | pass (exit 0) | EV-002 |
| TASK-0011/REQ-001 | TASK-0011/AC-002 | Review contains spec rows only | pass | EV-002 |

- EV-001: CI run #1, commit `652f938`, branch `main`, conclusion **success**. Jobs:
  `Spec coverage (documentary only)` success, `TypeScript` success,
  `Python (contracts/python)` success, `Python (services/worker)` success,
  `Generated artifacts match the schema` success. Queried via the GitHub Actions API.
- EV-002: `python scripts/check-spec-sync.py --base origin/main` exits 0 after `git fetch origin`.

**Worth stating plainly:** the expectation before this run was that something would fail, because
every check had only ever executed on Windows and the workflow itself was unproven. Nothing did.
That is a better outcome than predicted, not a vindication of the prediction. The pre-push check
for case-sensitive import paths, hardcoded Windows paths and lockfile currency found no problems,
which is the most likely reason the Linux run was clean.

## Final review

- Documentary coverage: PASS
- Spec → Code: ALIGNED — no code changed; specs now match observed reality.
- Code → Spec: ALIGNED — the reverse pass is what prompted this task. Four statements described a
  world without a remote and without a CI result; all four were corrected.
